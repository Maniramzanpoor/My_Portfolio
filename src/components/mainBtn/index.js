const MainBtn = ({prop}) => {
    return (
        <button className="w-max mx-1 rounded-md h-max px-5 py-3 bg-[#55198B] text-white font-black hover:bg-white hover:text-[#55198B] hover:border border-black transition-all duration-300	ease-in-out	">{prop}</button>
    );
}
 
export default MainBtn;